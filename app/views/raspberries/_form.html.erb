<%= form_for(@raspberry) do |f| %>
  <% if @raspberry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@raspberry.errors.count, "error") %> prohibited this raspberry from being saved:</h2>

      <ul>
      <% @raspberry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ip_address %><br>
    <%= f.text_field :ip_address %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
